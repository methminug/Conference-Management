import React from "react";
import {Container, Typography} from "@material-ui/core"

const SessionCard = () => {
    return(
        <Container>
            <Typography variant="h6">A session card</Typography>
        </Container>
    )
}

export default SessionCard;