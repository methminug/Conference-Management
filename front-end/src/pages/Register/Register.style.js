import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  background: {
    marginTop: "10px",
    width: "750px",
    height: "340px",
  },
  textFields: {
    marginTop: "20px",
  },
});
